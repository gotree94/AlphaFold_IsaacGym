# 작업 일지: 2026-01-27

## ASUS ROG Strix SCAR 16 G635LX | Ubuntu 22.04 LTS | RTX 5090

---

## 오늘 작업 요약

| 항목 | 상태 | 비고 |
|------|------|------|
| Isaac Lab + Isaac Sim | ✅ 해결 | PATH 충돌 수정 |
| ColabFold | ✅ 정상 | 조건부 PATH로 변경 |
| Alpamayo | ⏳ 미완료 | PyTorch 재설치 필요 |
| SSH 서버 | ✅ 설정 | MobaXterm 접속 가능 |

---

## 1. PATH 충돌 문제 해결

### 문제 상황

`conda activate isaaclab` 실행해도 ColabFold의 Python이 실행됨:

```bash
(isaaclab) $ which python
/home/gotree94/projects/alphafold/localcolabfold/.pixi/envs/default/bin/python  # 잘못됨
```

### 원인

`~/.bashrc`에서 ColabFold PATH가 conda보다 나중에 설정되어 덮어씀:

```bash
# 154-155번 줄 (문제의 원인)
export PATH="/home/gotree94/.pixi/bin:$PATH"
export PATH="$HOME/projects/alphafold/localcolabfold/.pixi/envs/default/bin:$PATH"
```

### 해결 방법

```bash
# 기존 PATH 라인 제거
sed -i '155d' ~/.bashrc
sed -i '154d' ~/.bashrc

# 조건부 PATH 추가
cat >> ~/.bashrc << 'EOF'

# Pixi (기본)
export PATH="$HOME/.pixi/bin:$PATH"

# ColabFold (alphafold 환경에서만)
activate-colabfold() {
    export PATH="$HOME/projects/alphafold/localcolabfold/.pixi/envs/default/bin:$PATH"
    cd ~/projects/alphafold
    echo "🧬 ColabFold activated"
}
EOF

source ~/.bashrc
```

### 확인

```bash
conda deactivate
source ~/.bashrc
conda activate isaaclab
which python
# 결과: /home/gotree94/miniconda3/envs/isaaclab/bin/python ✅
```

---

## 2. Isaac Lab 설치 확인

### 테스트 명령어

```bash
conda activate isaaclab
python -c "import isaacsim; print('Isaac Sim OK')"
```

### 시뮬레이션 테스트 (Headless)

```bash
cd ~/projects/isaaclab/repo
./isaaclab.sh -p scripts/tutorials/00_sim/create_empty.py --headless
```

출력:
```
[INFO]: Setup complete...
```

### 시각적 테스트 (GUI 모드)

```bash
# Cartpole 시뮬레이션 (GUI)
./isaaclab.sh -p scripts/tutorials/00_sim/spawn_prims.py

# 로봇 보기
./isaaclab.sh -p scripts/tutorials/01_assets/run_articulation.py

# Cartpole 학습 (GUI)
./isaaclab.sh -p scripts/reinforcement_learning/rsl_rl/train.py \
    --task=Isaac-Cartpole-v0 --num_envs=16
```

---

## 3. SSH 서버 설정

### 설치 및 활성화

```bash
# SSH 서버 설치
sudo apt install openssh-server -y

# 서비스 시작 및 자동 시작 설정
sudo systemctl enable ssh
sudo systemctl start ssh

# 상태 확인
sudo systemctl status ssh

# 방화벽 허용
sudo ufw allow ssh
```

### IP 주소 확인

```bash
hostname -I
```

### MobaXterm 접속 정보

- **Host**: `hostname -I` 결과
- **Username**: `gotree94`
- **Port**: `22`

---

## 4. 화면 녹화 (Ubuntu 내장)

### 단축키

| 기능 | 단축키 |
|------|--------|
| 녹화 시작/중지 | `Ctrl + Shift + Alt + R` |
| 전체 화면 캡쳐 | `PrtSc` |
| 영역 선택 캡쳐 | `Shift + PrtSc` |
| 현재 창 캡쳐 | `Alt + PrtSc` |

### 녹화 시간 제한 해제

```bash
gsettings set org.gnome.settings-daemon.plugins.media-keys max-screencast-length 0
```

### 저장 위치

`~/Videos/`

---

## 5. 남은 작업

### Alpamayo 재설치 필요

```bash
conda activate alpamayo
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}')"
```

### HuggingFace 인증 (Alpamayo 모델 다운로드용)

```bash
huggingface-cli login
```

웹에서 접근 요청:
- https://huggingface.co/nvidia/Alpamayo-R1-10B
- https://huggingface.co/datasets/nvidia/PhysicalAI-Autonomous-Vehicles

---

## 6. 환경별 사용법

### Isaac Lab

```bash
conda activate isaaclab
cd ~/projects/isaaclab/repo
./isaaclab.sh -p <스크립트> --headless
```

### ColabFold (AlphaFold)

```bash
activate-colabfold
colabfold_batch input.fasta output_dir/
```

### Alpamayo (설치 완료 후)

```bash
conda activate alpamayo
cd ~/projects/alpamayo/repo
source ar1_venv/bin/activate
python src/alpamayo_r1/test_inference.py
```

---

## 7. 시스템 검증 스크립트

```bash
~/bin/verify-all
```

현재 결과:
```
[1/7] System Info        ✅
[2/7] Disk Mounts        ✅
[3/7] NVIDIA & CUDA      ✅
[4/7] Docker GPU         ✅
[5/7] Isaac Lab          ✅ (해결됨)
[6/7] ColabFold          ✅
[7/7] Alpamayo           ⏳ (재설치 필요)
```

---

## 참고: 주요 경로

| 항목 | 경로 |
|------|------|
| Conda | `~/miniconda3/` |
| Isaac Lab | `~/projects/isaaclab/repo/` |
| ColabFold | `~/projects/alphafold/localcolabfold/` |
| Alpamayo | `~/projects/alpamayo/repo/` |
| 데이터/캐시 | `/mnt/workspace/` |

---

*작성일: 2026-01-27*
